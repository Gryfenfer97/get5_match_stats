<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta lang="en" />
    <title></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel="stylesheet" href="/assets/style.css">
  </head>

  <body class="vh-100">
    <nav class="navbar navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">
          <img src="/assets/logo.png" alt="Logo" height="24" class="d-inline-block align-text-top">
          Résultats CS:GO
        </a>
      </div>
    </nav>
    <main class="container position-relative h-90">
      <div class="position-absolute top-50 translate-middle-y w-100">
      <h1 class="pb-5"><%= team1_name %> - <%= team2_name %></h1>
      <div class="d-flex justify-content-evenly w-100">
      <% for(let map of maps) { %>
      <div class="card" style="width: 18rem;">
        <img src="/assets/maps/<%= map.mapname %>.jpg" class="card-img-top" alt="...">
        <div class="card-body">
          <h5 class="card-title">Map <%= map.mapnumber %></h5>
          <p class="card-text">
            <b><%= team1_name %></b> : <%= map.team1_score %><br>
            <b><%= team2_name %></b> : <%= map.team2_score %><br>
            <b>Durée</b>: <%= map.duration %>min
          </p>
          <a href="/match/<%= matchid %>/map/<%= map.mapnumber %>" class="stretched-link">Détails</a>
        </div>
      </div>
      <% } %>
      </div>
    </div>
    </main>
  </body>
</html>